package PerlLib::Amortizer;

# take inputs and collects a bunch and eventually feeds to a given
# system for simultaneous processing (to avoid lots of reloading)

# they can force a run

# they can set run times, minimum windows, etc, to determine how to
# load this

# eventually have workflow planning


1;

