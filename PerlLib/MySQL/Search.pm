package PerlLib::MySQL::Search;

# this is a system that if it doesn't already exist, performs various
# nice searches of MySQL databases (and perhaps others).  It gives a
# much more "AI" based approach to the problem.



1;
